@model IEnumerable<DISEASE_PREDICTION.Models.TBL_SPECIALIZATION>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SPECIALIZATION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SPECIALIZATION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SP_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SP_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SP_ID })
        </td>
    </tr>
}

</table>
